<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">

    <title>Redirection</title>
</head>
<body>

    <div class="container">

    <h1>Editar Link</h1>

    <form action="/edit/<%= body.id %>" method="POST">

        <div class="form-group my-2">
            <label>Título</label>
            <input type="text" class="form-control" name="title" placeholder="Ex: Instagram" value="<%= body.title %>" required>
        </div>

        <div class="form-group my-2">
            <label>Url</label>
            <input type="text" class="form-control" name="url" placeholder="https://exemplo.com/" value="<%= body.url %>" required>
        </div>

        <div class="form-group my-2">
            <label for="">Descrição</label>
            <textarea class="form-control" name="description" cols="10" placeholder="Link para o Perfil no Instagram"" rows="3" ><%= body.description %></textarea>
        </div>

        <button class="btn btn-primary btn-lg my-3">Salvar</button>

    </form>

        <% if(error) {%>

            <div class="alert alert-danger alert-dismissible fade show mt-3 w-50">

                <strong><a href="#" class="text-danger text-decoration-none float-end" data-bs-dismiss="alert">&times;</a></strong>
                <strong>Error: </strong><%=error.message%>

            </div>

        <% } %>

    </div>

    </div>

</body>
</html>